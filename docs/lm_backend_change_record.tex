\documentclass[11pt,a4paper]{article}

\usepackage[T1]{fontenc}
\usepackage[margin=2.5cm]{geometry}
\usepackage{amsmath}
\usepackage{array}
\usepackage{hyperref}

\title{Native LM Backend: Project Change Record}
\date{3 September 2026}

\begin{document}

\maketitle

\section{Purpose of This Record}

This document records every project-specific change made while integrating the
native Languasco--Migliardi (LM) log-likelihood implementation into LM-IDNet.
It is intended to make the integration reviewable and to provide a concise
account that can be reported to the authors of the native implementation.

The original reference copy under \path{lm-algorithm/code/} is kept unchanged.
All integration edits are made to the runtime project copy under
\path{src/lm_idnet/algorithms/native/}.

\section{Baseline Before Native Changes}

Before modifying the native source, the following checks were completed:

\begin{itemize}

\item The focused likelihood and estimator-factory tests passed: 5 passed.

\item The normal project suite passed: 195 passed and 10 tests were deselected
by the configured test markers.

\item The runtime C sources, headers, and coefficient tables were compared with
the reference capsule.

\item The two copies of \path{LM_time_lib.c} were identical before the first
edit, with SHA-256 digest:

\begin{verbatim}
306b33aae05db49e519909c36e91d25112b5fce75a3fb144858ed0d727f22d66
\end{verbatim}

\end{itemize}

\section{Change 001: Exact Handling of a Zero Count}

\subsection{Status}

Implemented and directly validated against a temporary shared library compiled
from the modified source.

\subsection{File Changed}

\begin{verbatim}
src/lm_idnet/algorithms/native/LM_time_lib.c
\end{verbatim}

The corresponding reference file was not changed:

\begin{verbatim}
lm-algorithm/code/LM_time_lib.c
\end{verbatim}

After this change, the digests are:

\begin{verbatim}
Project copy:
e948740488ed6ef731a7eabc7f02dcdbd83f666fcfbd793010ac14003bdf5e15

Reference copy:
306b33aae05db49e519909c36e91d25112b5fce75a3fb144858ed0d727f22d66
\end{verbatim}

\subsection{Problem Being Addressed}

The function \texttt{LM\_logL} evaluates a paired log-gamma difference. In the
application parameterization, its mathematical contribution is of the form

\[
D(\alpha,y)=\log\Gamma(\alpha+y)-\log\Gamma(\alpha),
\]

where \(y\) is a non-negative packet count. Network-traffic windows commonly
contain categories for which \(y=0\). A completely silent window also has a
total count \(N=0\).

For zero counts, the exact value is

\[
D(\alpha,0)
=\log\Gamma(\alpha)-\log\Gamma(\alpha)
=0.
\]

The supplied implementation already contained exact special cases for
\(y=1\) and \(y=2\), but it did not contain the corresponding base case for
\(y=0\). Sending zero through the general Euler--Maclaurin expression is
unnecessary and does not directly express this exact identity.

\subsection{Applied Modification}

The following guard was inserted at the beginning of the existing special-value
section in \texttt{LM\_logL}, before the cases for one and two:

\begin{verbatim}
if (y == 0)
    {
    return 0.0;
    }
\end{verbatim}

The semantic source diff is:

\begin{verbatim}
 /* special values */
+if (y == 0)
+    {
+    return 0.0;
+    }
 if (y == 1)
\end{verbatim}

The patching tool also normalized one redundant blank line at the end of the C
file. That whitespace-only difference has no effect on compilation or behavior.

\subsection{Why the Change Was Made in This Function}

Handling the case in \texttt{LM\_logL} covers both places where a zero can
occur:

\begin{itemize}

\item a category contribution with \(x_k=0\); and

\item the concentration contribution for a completely silent row with
\(N=0\).

\end{itemize}

Placing the guard in the shared paired-difference function avoids duplicating
zero checks in the future matrix wrapper or in Python. It also ensures that any
other caller of \texttt{LM\_logL} receives the same exact behavior.

\subsection{Effect on the LM Numerical Method}

This change does not modify:

\begin{itemize}

\item the Euler--Maclaurin expansion;

\item coefficient values;

\item truncation-order selection;

\item error-bound calculations;

\item horizontal shifting; or

\item results for positive counts.

\end{itemize}

It only returns a known exact identity before the approximation is entered.

\subsection{Native Verification}

The host development environment did not contain a C compiler. The modified
source was therefore compiled using the existing \texttt{lm-idnet:latest}
Docker image, which is the supported project toolchain. The shared object was
written only to a temporary directory; the versioned project binary was not
replaced.

The compiler invocation was equivalent to:

\begin{verbatim}
gcc -O3 -fPIC -shared LM_time_lib.c -lm -o LM_time_lib.so
\end{verbatim}

The compiled \texttt{LM\_logL} function was then called through
\texttt{ctypes} with three different positive reciprocal parameters, expansion
orders, and horizontal-shift values. All three calls returned exactly zero:

\begin{verbatim}
zero-count values: [0.0, 0.0, 0.0]
\end{verbatim}

A four-category silent row was also evaluated by combining the concentration
term and four category terms in the same form as the native likelihood:

\begin{verbatim}
silent-row kernel: 0.0
\end{verbatim}

The existing focused Python tests were rerun after the source edit:

\begin{verbatim}
tests/test_likelihood.py          4 passed
tests/test_estimator_factory.py   1 passed
Total                             5 passed
\end{verbatim}

The normal project suite was also rerun after the native change:

\begin{verbatim}
195 passed, 10 deselected
\end{verbatim}

Finally, \texttt{git diff --check} reported no whitespace errors.

\subsection{Current Limitation}

This change has been tested directly at the native function boundary, but the
public Python LM backend is still a placeholder. An end-to-end Python test for
silent matrices will be added when the native adapter and matrix entry point are
implemented. Until then, the modified shared object is compiled only for direct
verification and is not used by normal application commands.

\subsection{Short Report for the Native-Code Authors}

The project copy adds an exact \(y=0\) base case to
\texttt{LM\_logL}. The mathematical paired log-gamma difference is exactly zero
for this input. The guard is necessary for sparse protocol vectors and fully
silent IoT traffic windows. It does not alter any positive-count calculation or
any part of the LM approximation and error-control method. The original capsule
source remains unchanged.

\section{Change 002: Explicit Paths and Checked Coefficient Loading}

\subsection{Status}

Implemented and validated at the native shared-library boundary. The Python adapter that
will translate the returned statuses into application exceptions remains a later
integration step.

\subsection{Files Changed}

\begin{verbatim}
src/lm_idnet/algorithms/native/LM_time_lib.c
src/lm_idnet/algorithms/native/LM_hor_shift.h
\end{verbatim}

The corresponding files under \path{lm-algorithm/code/} were not changed.

\subsection{Problem Being Addressed}

The supplied \texttt{initBern\_logL} function opened the two coefficient files by
bare filename. Its success therefore depended on the process current working
directory. It also wrote through allocated pointers without checking whether
the files opened, whether allocation succeeded, or whether all coefficients
were read. Used as a library, these failure modes could cause invalid state or
a process crash.

\subsection{Applied Modification}

A checked initializer was added and declared in \path{LM_hor_shift.h}:

\begin{verbatim}
int initBern_logL_paths(
    const char *bernoulli_path,
    const char *error_path
);
\end{verbatim}

This function:

\begin{enumerate}

\item rejects null path pointers;

\item opens the two caller-supplied paths;

\item allocates temporary 100-element arrays;

\item verifies all 100 reads from each file;

\item closes both files on every path after they have been opened;

\item preserves the previously loaded tables if opening, allocation, or
parsing fails; and

\item replaces and releases the old tables only after both new tables have
been read successfully.

\end{enumerate}

The return statuses are:

\begin{center}
\begin{tabular}{>{\ttfamily}l p{10cm}}
0 & both tables loaded successfully; \\
1 & a path was null or a file could not be opened; \\
2 & memory allocation failed; \\
3 & a file did not contain all 100 parseable coefficients. \\
\end{tabular}
\end{center}

The original no-argument \texttt{initBern\_logL} symbol remains available for
compatibility. It delegates to the checked function with the original relative
filenames and writes one diagnostic to standard error if loading fails. The
new Python integration will call \texttt{initBern\_logL\_paths} with absolute
paths instead.

Only the following tables are loaded by this likelihood-specific initializer:

\begin{verbatim}
bernreal-norm-100.txt
err_coeff-100.txt
\end{verbatim}

No coefficient value or file was modified.

\subsection{Native Verification}

The modified library was loaded after changing the test process current
directory to \path{/tmp}. Initialization with absolute paths succeeded. The
following failure cases returned the expected nonzero status without crashing:

\begin{itemize}

\item two null paths: status 1;

\item a missing file: status 1; and

\item a nonnumeric file in place of a coefficient table: status 3.

\end{itemize}

After a valid load, an attempted malformed reload returned status 3, and a
subsequent likelihood calculation still succeeded. This confirms that a failed
reload does not destroy the previously valid coefficient state.

The initializer was also called successfully 1,000 times. Process resident
memory remained at 11,644 KiB before and after those reloads in this stress
check.

\subsection{Effect on the LM Numerical Method}

This change affects file location, validation, and table ownership only. It
does not change the coefficient files, their in-memory values, or any LM
formula.

\subsection{Short Report for the Native-Code Authors}

The project copy adds an explicit-path, status-returning initializer for the two
tables used by the log-likelihood path. It reads into temporary storage and
commits the replacement only after both complete tables have been validated.
The original initializer remains as a compatibility wrapper. The change removes
the current-working-directory dependency and makes initialization failures
recoverable by a library caller.

\section{Change 003: Explicit Workspace Ownership and Allocation Checks}

\subsection{Status}

Implemented for every allocation exercised by the current parameter and
log-likelihood path. Final cleanup after a matrix calculation will be connected
when the matrix-level entry point is added; that entry point does not yet exist.

\subsection{Problem Being Addressed}

The native implementation retained four parameter arrays in global pointers.
Every call to \texttt{init\_params} replaced those pointers with new allocations
without releasing the preceding arrays. In addition,
\texttt{opterrNoPrint\_logL} allocated two temporary arrays on every likelihood
calculation and did not release them. Repeated fitting iterations would
therefore continually increase native memory use.

The allocation results were not checked before their pointers were used.

\subsection{Applied Modifications}

A private \texttt{free\_params} helper now releases the arrays referenced by
\texttt{X}, \texttt{probs}, \texttt{psioverprobs}, and \texttt{logprobs}, then
sets all four pointers to \texttt{NULL}. The parameter initializer invokes it
before allocating a replacement workspace and again if any replacement
allocation fails.

\texttt{init\_params} now rejects a null count pointer, a nonpositive category
count, or a nonpositive precision value by returning \texttt{-1}. It also
returns \texttt{-1} if any of its four allocations fails. No pointer is written
through after a failed allocation.

Within \texttt{opterrNoPrint\_logL}, the two category-length temporary arrays
were replaced by one allocation of \texttt{2*K} doubles, divided into two
non-overlapping halves. This keeps the same values and indexing while requiring
only one allocation check. The block is freed on both existing return paths:

\begin{itemize}

\item failure to meet the requested accuracy within the horizontal-shift
limit; and

\item successful selection of the expansion order and horizontal shift.

\end{itemize}

If that temporary allocation fails, or if the requested accuracy cannot be met
within the existing horizontal-shift limit, \texttt{opterrNoPrint\_logL}
returns \texttt{NULL}. \texttt{loggamma\_LM} checks that return and propagates
the failure as \texttt{NAN}. The future matrix wrapper can therefore reject the
non-finite value rather than mistaking the failure for a valid likelihood
result.

\texttt{loggamma\_LM} also invokes \texttt{free\_params} before returning
its existing precision-limit sentinel. Thus, both failure returns currently
visible in that function release the active parameter workspace.

The coefficient arrays have a separate, longer lifetime. The private
\texttt{freeBern\_logL} helper releases and nulls those two pointers only when
a complete replacement pair has been loaded successfully.

\subsection{Ownership After This Change}

\begin{center}
\begin{tabular}{p{4.2cm} p{5.3cm} p{4.0cm}}
\textbf{Storage} & \textbf{Lifetime} & \textbf{Released by} \\
\hline
Coefficient tables & Across likelihood calls, until a successful reload &
\texttt{freeBern\_logL} before replacement \\
Parameter arrays & From successful \texttt{init\_params} until replacement or
the future matrix boundary & \texttt{free\_params} \\
Error-search workspace & One \texttt{opterrNoPrint\_logL} call & The function
itself on success or failure \\
\end{tabular}
\end{center}

\subsection{Deliberately Deferred Connection Point}

There is not yet a matrix-level native function, so there is currently no
``last row'' boundary at which to invoke \texttt{free\_params}. The helper is
private in the same C translation unit and is ready to be called from the
matrix wrapper in the next implementation step. Until then, the most recent
parameter workspace remains live for the next \texttt{loggamma\_LM} call and is
released before the next \texttt{init\_params} call. This preserves the calling
sequence expected by the supplied API without introducing a speculative public
cleanup API.

\subsection{Native Verification}

The following checks were made through \texttt{ctypes} against a temporary
shared library:

\begin{itemize}

\item valid initialization and likelihood evaluation produced the finite
value \texttt{-8.923657799857487};

\item null counts, zero categories, and zero precision were rejected with
\texttt{-1};

\item the existing precision-limit path returned its documented value of 99,
after which parameter initialization and a normal likelihood call succeeded;

\item 20,000 alternating three-category and 256-category initializations
increased resident memory by only 68 KiB (11,576 to 11,644 KiB); and

\item 10,000 repeated 256-category likelihood evaluations returned the same
finite value each time while resident memory remained at 11,644 KiB.

\end{itemize}

The small resident-memory differences are consistent with allocator and process
bookkeeping; importantly, growth was not proportional to the number of calls.

\subsection{Effect on the LM Numerical Method}

The contents, length, and indexing of the working arrays are unchanged. No
formula, loop termination condition, coefficient, error bound, expansion order,
or horizontal-shift rule was changed.

\subsection{Short Report for the Native-Code Authors}

The project copy now gives the parameter arrays and per-likelihood error-search
arrays explicit owners. Existing arrays are released before replacement,
temporary arrays are released on all returns, pointers are nulled after release,
and allocations are checked. Repeated calls no longer leak these workspaces.
The final parameter cleanup call will sit in the forthcoming matrix wrapper,
where the end of a calculation is unambiguous.

\section{Consolidated Verification After Changes 001--003}

The source was compiled using the existing project Docker image with
\texttt{gcc -O3 -Wall -Wextra -fPIC -shared} and linked with
\texttt{-lm}. Compilation succeeded. The reported warnings concern unused
parameters or variables already present in the supplied implementation; no
invalid declaration, missing symbol, or linkage warning was reported.

The Python regression results after Changes 001--003 are:

\begin{verbatim}
Focused likelihood/factory tests: 5 passed
Normal project suite:             195 passed, 10 deselected
git diff --check:                 passed
\end{verbatim}

The final SHA-256 digests at this checkpoint are:

\begin{verbatim}
Runtime LM_time_lib.c:
8ddea61941d64977562c640c8264d75d5127ff353150240be4c019f9fe6b6c3d

Reference LM_time_lib.c (unchanged):
306b33aae05db49e519909c36e91d25112b5fce75a3fb144858ed0d727f22d66

Runtime LM_hor_shift.h:
1ce8a17c4a50057f4d92a6cbf580045320ba221d6b4883236f8bbbbb2d6580fa

Reference LM_hor_shift.h (unchanged):
3ba1ac510b0fba74a64ae4bc63be5301c214ee783a2fbde0e883188f32a3be96

bernreal-norm-100.txt (unchanged):
5072a78dbf4af4e317704e714d25fa4ea559bbb169d2652a60328381553e09dd

err_coeff-100.txt (unchanged):
48eb77cf4c9527a204022899469a1877fd667fc53d38ab164fabf1a0d6adebf6
\end{verbatim}

At the Changes 001--003 checkpoint, the versioned \path{LM_time_lib.so} had not
yet been replaced. All native checks at that checkpoint used a temporary shared
library. Change 005 below records the subsequent controlled rebuild.

\section{Change 004: Matrix-Level Log-Likelihood Entry Point}

\subsection{Status}

Implemented, exported, and validated against SciPy and direct failure cases.

\subsection{Files Changed}

\begin{verbatim}
src/lm_idnet/algorithms/native/LM_time_lib.c
src/lm_idnet/algorithms/native/LM_hor_shift.h
\end{verbatim}

The corresponding files under \path{lm-algorithm/code/} remain unchanged.

\subsection{Application-Facing Interface}

The following function was added to the runtime C source and declared in the
runtime header:

\begin{verbatim}
double loggamma_LM_matrix(
    int precision_digits,
    const double *counts,
    long rows,
    int categories,
    const double *probabilities,
    double psi
);
\end{verbatim}

The count matrix is interpreted as contiguous row-major \texttt{double}
storage. Every row is one independent observation. The function evaluates each
row with the supplied LM routine and returns the sum; it does not aggregate the
count vectors before evaluation.

\subsection{Boundary Validation}

The wrapper returns \texttt{NAN} without entering the LM calculation if any of
the following conditions is found:

\begin{itemize}

\item the requested precision or row count is nonpositive;

\item there are fewer than two categories;

\item the count or probability pointer is null;

\item either required coefficient table is uninitialized;

\item \(\psi\) is non-finite or nonpositive;

\item a probability is non-finite or nonpositive;

\item the probabilities do not sum to one within an absolute tolerance of
\(10^{-12}\); or

\item a count is non-finite, negative, or non-integral.

\end{itemize}

The probability-sum and count checks were added at the native trust boundary so
that callers other than the planned Python adapter cannot send values for which
the Dirichlet--Multinomial parameterization is undefined.

\subsection{Row Evaluation and Failure Semantics}

For every row, the wrapper:

\begin{enumerate}

\item calls the checked \texttt{init\_params} function with that row;

\item evaluates it through the existing \texttt{loggamma\_LM} function;

\item rejects the existing precision-limit sentinel value 99;

\item rejects any non-finite row value; and

\item adds the finite row value to the matrix total and verifies that the total
remains finite.

\end{enumerate}

One cleanup label calls \texttt{free\_params} on every wrapper return, including
successful completion, invalid input, initialization failure, precision
failure, horizontal-shift failure, and non-finite output. This completes the
``last matrix row'' ownership connection planned in Change 003.

The wrapper returns the finite sum on success and \texttt{NAN} for every
failure. This deliberately provides one unambiguous failure value for the
future Python adapter.

\subsection{Removal of Reachable Native Output}

Two existing numerical-failure branches were reachable from the new wrapper:

\begin{itemize}

\item failure to find a sufficient horizontal shift; and

\item failure of the existing double-precision magnitude check.

\end{itemize}

Their \texttt{printf} and \texttt{fprintf} calls were removed from the runtime
project copy. Their return behavior remains available programmatically:
\texttt{opterrNoPrint\_logL} returns \texttt{NULL}, and
\texttt{loggamma\_LM} retains its existing value 99 for the precision check.
The matrix wrapper converts both paths to \texttt{NAN}. It therefore does not
print, call \texttt{exit}, or terminate the host Python process.

No other functions containing active printing or process termination are
called by \texttt{loggamma\_LM\_matrix}. Legacy dataset-file initializers were
not modified because they are outside the application call path.

\subsection{Effect on the LM Numerical Method}

The new function only validates, sequences, sums, and cleans up calls to the
existing row algorithm. It does not change:

\begin{itemize}

\item the conversion between probabilities, \(\psi\), and Dirichlet
parameters;

\item the Euler--Maclaurin expansion;

\item any coefficient;

\item error-bound calculations;

\item expansion-order selection; or

\item the horizontal-shift search.

\end{itemize}

\subsection{Native Verification}

For the matrix

\[
X=
\begin{pmatrix}
2 & 0\\
1 & 1
\end{pmatrix},
\qquad
\boldsymbol{\alpha}=(1,1),
\]

the wrapper returned:

\begin{verbatim}
LM matrix result:       -2.8903717578961645
SciPy row-wise result:  -2.8903717578961645
Aggregated-row result:  -2.9957322735610155
\end{verbatim}

The equality with the row-wise reference and the different aggregated result
confirm that observations are evaluated independently. A three-row silent
matrix returned exactly zero.

Direct checks also confirmed that the wrapper returns \texttt{NAN} for an
uninitialized coefficient state, null pointers, invalid dimensions,
nonpositive precision, invalid \(\psi\), unnormalized probabilities, negative
counts, fractional counts, and the existing precision-limit condition. A valid
call after those failures returned the original reference result, confirming
that no stale parameter workspace remained.

A lifecycle stress check made 10,000 matrix calls while alternating between
shapes (2\times4) and (64\times4). Process resident memory changed from
27,372 KiB to 27,464 KiB and did not grow in proportion to the call or row
count.

A separate larger-count comparison was made against 80-decimal-digit arithmetic.
At a request of eight significant digits, the absolute difference was about
\(1.03\times10^{-7}\) on a result of magnitude 390.61, corresponding to a
relative difference of about \(2.64\times10^{-10}\). This is within eight
significant digits but illustrates why validation should use the implementation's
significant-digit interpretation rather than treating the setting as a fixed
number of decimal places. No numerical method was changed in response to this
observation.

\subsection{Persistent Regression Check}

A direct native contract test was added:

\begin{verbatim}
tests/test_lm_native_matrix.py
\end{verbatim}

It loads the rebuilt shared object, initializes coefficients by absolute path,
compares the matrix result with \texttt{ScipyLogLikelihood}, proves that rows
are not aggregated, checks a silent matrix, and checks representative invalid
input and precision failures.

\subsection{Short Report for the Native-Code Authors}

The runtime project copy now exposes a matrix-level library function. It calls
the supplied LM likelihood once per observation and sums the results. The
wrapper validates the native boundary, translates the existing failure signals
to \texttt{NAN}, and releases parameter memory on every return. Diagnostic
printing was removed from the two numerical-failure branches reachable through
this interface so that it is safe to call from Python. The LM approximation and
its error-control mathematics were not modified.

\section{Change 005: Rebuilt Runtime Shared Library}

\subsection{Status}

Completed using the project's existing Docker compiler and entry point. No new
build system or dependency was introduced.

\subsection{Build Procedure}

The repository was mounted at \path{/app} in the existing
\texttt{lm-idnet:latest} image and the standard container entry point was run.
That entry point compiled the native source through its temporary-output path
and atomically replaced the runtime library using the equivalent of:

\begin{verbatim}
gcc -O3 -fPIC -shared LM_time_lib.c -lm -o .LM_time_lib.so.tmp
mv .LM_time_lib.so.tmp LM_time_lib.so
\end{verbatim}

The container was run with the host numeric user and group identifiers so the
rebuilt artifact retained normal repository ownership.

\subsection{Artifact Verification}

The rebuilt file is a dynamically linked, 64-bit x86-64 ELF shared object. Its
dynamic symbol table contains both required integration symbols:

\begin{verbatim}
initBern_logL_paths
loggamma_LM_matrix
\end{verbatim}

Loading the versioned shared object with \texttt{ctypes} and executing the new
persistent contract test succeeded.

An additional diagnostic build with \texttt{-Wall -Wextra} succeeded. Its
warnings concerned unused parameters and variables already present in the
supplied implementation. There were no missing declarations, missing symbols,
or math-library linkage errors introduced by the integration.

The complete project regression suite after the source and binary rebuild
reported:

\begin{verbatim}
196 passed, 10 deselected
\end{verbatim}

The additional passing test is the new native matrix contract check.

\subsection{Digests After Changes 004--005}

\begin{verbatim}
Runtime LM_time_lib.c:
60963e19d7e17daa8901192187142ba581a240064652286742ac2d1dcb550a05

Reference LM_time_lib.c (unchanged):
306b33aae05db49e519909c36e91d25112b5fce75a3fb144858ed0d727f22d66

Runtime LM_hor_shift.h:
824c2c72cddd983cd5c0d44809fd82e40dbd172e5756aba8b788fb2eada6dd0f

Reference LM_hor_shift.h (unchanged):
3ba1ac510b0fba74a64ae4bc63be5301c214ee783a2fbde0e883188f32a3be96

Previous runtime LM_time_lib.so:
553eb6bf0826501e2b6aacb26887d3a530b24846fa7c3d5804734e94d8cdefc2

Rebuilt runtime LM_time_lib.so:
74c827713a213f0f750cf24319e0257fb2df595f530684bf9488b62c5b3d081c
\end{verbatim}

\section{Change 006: Thread-Safe Python Native Adapter}

\subsection{Status}

Implemented and validated directly. The adapter is not yet selected by
\texttt{LmLogLikelihood}; that application-facing connection is the next
integration step.

\subsection{Files Changed}

\begin{verbatim}
src/lm_idnet/algorithms/lm_native.py
tests/test_lm_native_matrix.py
docs/lm_backend_change_record.tex
\end{verbatim}

No native C source, header, coefficient file, or shared object was modified in
this step. In particular, the reference files under \path{lm-algorithm/code/}
remain unchanged.

\subsection{Adapter Structure}

The previously empty \path{lm_native.py} module now defines the
\texttt{NativeLmKernel} class. It is the only Python source or test module that
imports \texttt{ctypes} or directly names the two native integration functions.

The module contains:

\begin{itemize}

\item an absolute native directory derived from the module's
\texttt{\_\_file\_\_} path;

\item one shared \texttt{double *} declaration;

\item one process-wide \texttt{threading.Lock}; and

\item the small adapter class.

\end{itemize}

No new dependency, loader abstraction, or build mechanism was added.

\subsection{Construction and Initialization}

\texttt{NativeLmKernel} first requires \texttt{precision\_digits} to be a
positive Python integer. Boolean, floating-point, zero, and negative values are
rejected with \texttt{DataValidationError} before native loading begins.

The constructor loads this exact path:

\begin{verbatim}
src/lm_idnet/algorithms/native/LM_time_lib.so
\end{verbatim}

It then declares the \texttt{argtypes} and \texttt{restype} contracts for:

\begin{verbatim}
initBern_logL_paths
loggamma_LM_matrix
\end{verbatim}

An absent shared object, loader failure, or missing required symbol is translated
to \texttt{CommandUnavailableError}. This prevents a low-level
\texttt{OSError} or \texttt{AttributeError} from escaping into the application.

While holding the process-wide lock, the constructor calls
\texttt{initBern\_logL\_paths} with filesystem-encoded absolute paths to the two
required coefficient files. A nonzero native status is translated to
\texttt{CommandUnavailableError} and the status value is retained in the
message.

The absolute module-relative paths were validated after changing the Python
process current directory to an unrelated temporary directory.

\subsection{Calculation Path}

For each call, \texttt{NativeLmKernel.calculate}:

\begin{enumerate}

\item converts the count matrix to contiguous NumPy \texttt{float64} storage;

\item calculates

\[
\alpha_0=\sum_k\alpha_k,
\qquad
p_k=\frac{\alpha_k}{\alpha_0},
\qquad
\psi=\frac{1}{\alpha_0};
\]

\item converts the probability vector to contiguous NumPy \texttt{float64}
storage;

\item calls \texttt{loggamma\_LM\_matrix} while holding the same process-wide
lock used for coefficient initialization;

\item converts the returned value to a Python \texttt{float}; and

\item translates a non-finite native result to
\texttt{NumericalPrecisionError}, including the requested significant-digit
setting in the message.

\end{enumerate}

The lock covers every native initialization and matrix calculation because the
shared object stores coefficient, dimension, parameter, and workspace state in
global variables. It does not attempt to make the professor's native code
internally concurrent.

\subsection{Validation Boundary}

The adapter assumes that its NumPy inputs have already passed the common
likelihood validation. Connecting that validator is intentionally reserved for
\texttt{LmLogLikelihood} in the next step. The C matrix wrapper still rejects
invalid pointers and numerical inputs defensively, and any resulting
\texttt{NAN} becomes \texttt{NumericalPrecisionError} here.

This separation avoids duplicating dimension, whole-count, non-negativity, and
positive-alpha checks in both Python layers.

\subsection{Verification}

The existing matrix test was refactored to call \texttt{NativeLmKernel} rather
than importing \texttt{ctypes}. Its coverage now includes:

\begin{itemize}

\item row-wise agreement with the SciPy backend;

\item a non-contiguous input matrix, proving that the adapter creates contiguous
storage;

\item a different result from an intentionally aggregated matrix;

\item an exactly zero silent matrix;

\item rejection of four invalid precision values;

\item missing-library and missing-coefficient errors;

\item translation of a native precision failure; and

\item 200 calls through four Python worker threads, alternating parameter and
count inputs, with every result matching its SciPy reference.

\end{itemize}

The focused adapter test result was:

\begin{verbatim}
8 passed
\end{verbatim}

The complete project regression suite reported:

\begin{verbatim}
203 passed, 10 deselected
\end{verbatim}

A source scan confirmed that \path{lm_native.py} is the only Python module
containing \texttt{ctypes}, \texttt{initBern\_logL\_paths}, or
\texttt{loggamma\_LM\_matrix} references.

\subsection{Digests After Change 006}

\begin{verbatim}
lm_native.py:
4ebfd2340e52e925dcdd32762d93d6cb12f99f1a98aa36e908afe87e4c0d7a53

test_lm_native_matrix.py:
5a16f57b8eda9a9c0ca2a23687c507755bc4e677555d1170357a1185f76ba8ae

Runtime LM_time_lib.c (unchanged in this step):
60963e19d7e17daa8901192187142ba581a240064652286742ac2d1dcb550a05

Runtime LM_hor_shift.h (unchanged in this step):
824c2c72cddd983cd5c0d44809fd82e40dbd172e5756aba8b788fb2eada6dd0f

Runtime LM_time_lib.so (unchanged in this step):
74c827713a213f0f750cf24319e0257fb2df595f530684bf9488b62c5b3d081c
\end{verbatim}

\subsection{Short Report for the Native-Code Authors}

This step adds only the Python boundary around the native matrix function. It
loads the project shared object and coefficient files by absolute path,
serializes all access to the native global state, converts Dirichlet parameters
to the probability/\(\psi\) representation, and maps load and numerical failures
to application exceptions. No native algorithm or source file was changed.

\section{Change 007: Activate the LM Likelihood Backend}

\subsection{Status}

Implemented and validated. The placeholder behavior has been removed and the
public likelihood factory can now construct a working native LM backend.

\subsection{Files Changed}

\begin{verbatim}
src/lm_idnet/algorithms/log_likelihood.py
tests/test_likelihood.py
docs/lm_backend_change_record.tex
\end{verbatim}

No native C source, header, coefficient table, shared object, or reference file
was modified in this step.

\subsection{Backend Activation}

\texttt{LmLogLikelihood} now owns a \texttt{NativeLmKernel}. Its constructor
accepts \texttt{precision\_digits} and retains the current project default of
six significant digits:

\begin{verbatim}
LmLogLikelihood(precision_digits=6)
\end{verbatim}

Its \texttt{calculate} method converts counts and alpha to NumPy arrays, invokes
the existing \texttt{ScipyLogLikelihood.\_validate\_inputs} method, and delegates
the validated arrays to \texttt{NativeLmKernel.calculate}.

Reusing the existing validator gives both backends the same checks for:

\begin{itemize}

\item a nonempty two-dimensional count matrix;

\item matching alpha and category dimensions;

\item finite, non-negative, whole-number counts; and

\item finite, strictly positive alpha values.

\end{itemize}

No validation rule was copied into a second implementation.

\subsection{Factory Interface}

The public factory now accepts an optional precision argument:

\begin{verbatim}
initialize_log_likelihood(backend, precision_digits=6)
\end{verbatim}

For the \texttt{lm} selection, the factory passes this value to
\texttt{LmLogLikelihood}. For the \texttt{scipy} selection, it continues to use
the parameterless SciPy constructor because SciPy does not use this setting.

Existing one-argument callers remain compatible and currently receive six
digits. Passing \texttt{config.precision\_digits} through every application
construction path remains the next planned step; consequently, a non-default
configuration value is not yet propagated by those callers.

An unknown backend continues to raise the existing \texttt{DataValidationError}
without attempting native initialization.

\subsection{Verification}

The likelihood tests now confirm that:

\begin{itemize}

\item \texttt{initialize\_log\_likelihood("lm", precision\_digits=6)} returns a
working \texttt{LmLogLikelihood};

\item the constructed backend retains the requested precision;

\item its multirow result matches the SciPy kernel for a known case;

\item invalid fractional counts are rejected by the shared Python validator;
and

\item an unknown backend retains its previous validation failure.

\end{itemize}

The focused likelihood and native-adapter suites reported:

\begin{verbatim}
14 passed
\end{verbatim}

The complete project regression suite reported:

\begin{verbatim}
205 passed, 10 deselected
\end{verbatim}

\subsection{Digests After Change 007}

\begin{verbatim}
log_likelihood.py:
b23b1aaa42250f1b62d07a9ae30fd448b98a46871d4b4bd2257db6a57d3b4982

test_likelihood.py:
90b8e0b7c390fac5e29ff1b84cbe5526d27383142c710b43c98c071267a8fef0

lm_native.py (unchanged in this step):
4ebfd2340e52e925dcdd32762d93d6cb12f99f1a98aa36e908afe87e4c0d7a53

Runtime LM_time_lib.c (unchanged in this step):
60963e19d7e17daa8901192187142ba581a240064652286742ac2d1dcb550a05

Runtime LM_time_lib.so (unchanged in this step):
74c827713a213f0f750cf24319e0257fb2df595f530684bf9488b62c5b3d081c
\end{verbatim}

\subsection{Short Report for the Native-Code Authors}

This step changes only the application-facing Python likelihood selection. The
LM backend now validates inputs using the same rules as the SciPy reference and
then delegates to the previously documented native adapter. No native source,
coefficient, numerical method, or compiled artifact was changed.

\section{Change 008: Propagate Precision Through Every Construction Path}

\subsection{Status}

Implemented and validated across estimator construction, model training, daily
diagnostics, threshold calibration, and development-window scoring.

\subsection{Files Changed}

\begin{verbatim}
src/lm_idnet/algorithms/estimator_factory.py
src/lm_idnet/cli.py
src/lm_idnet/models/modeling_stage.py
src/lm_idnet/models/calibration_stage.py
src/lm_idnet/models/scoring_stage.py
tests/test_estimator_factory.py
tests/test_cli.py
tests/test_modeling_stage.py
tests/test_calibration_stage.py
tests/test_scoring_stage.py
docs/lm_backend_change_record.tex
\end{verbatim}

No native source, header, coefficient table, shared object, or reference file
was modified.

\subsection{Single Precision Source}

The existing top-level \texttt{AppConfig.precision\_digits} value remains the
only application precision setting. No second setting was added to
\texttt{EstimatorConfig} or any model stage.

\texttt{create\_estimator} now accepts an optional precision argument and
passes it to \texttt{initialize\_log\_likelihood}:

\begin{verbatim}
create_estimator(config, precision_digits=6)
\end{verbatim}

The default preserves existing direct callers and tests. Application call sites
now pass the validated top-level configuration value explicitly.

\subsection{Updated Construction Paths}

The completed wiring is:

\begin{center}
\begin{tabular}{p{3.8cm} p{6.0cm} p{4.5cm}}
\textbf{Operation} & \textbf{Backend source} & \textbf{Precision source} \\
\hline
Model training & \texttt{config.estimator.log\_likelihood\_backend} &
\texttt{config.precision\_digits} \\
Daily diagnostics & \texttt{config.estimator.log\_likelihood\_backend} &
\texttt{config.precision\_digits} \\
Threshold calibration & fitted model artifact &
\texttt{config.precision\_digits} \\
Window scoring & fitted model artifact &
\texttt{config.precision\_digits} \\
\end{tabular}
\end{center}

The training stage and diagnostic command pass both estimator configuration and
precision through \texttt{create\_estimator}. Calibration and scoring pass the
backend recorded by the fitted model plus the current validated precision
directly to \texttt{initialize\_log\_likelihood}.

Calibration and scoring deliberately do not use the current estimator backend
selection. This preserves artifact compatibility: a model fitted with LM is
evaluated with LM even if the current configuration's estimator selection is
different.

\subsection{Verification}

The focused wiring tests verify the following behavior:

\begin{itemize}

\item the estimator factory constructs \texttt{LmLogLikelihood} with an
explicit precision of eight;

\item model training forwards the top-level precision value to the estimator
factory;

\item daily diagnostics forwards both the configured \texttt{lm} selection and
precision value;

\item calibration reads \texttt{lm} from the fitted model artifact while
reading precision eight from the current configuration; and

\item scoring uses the same artifact-backend/configuration-precision split.

\end{itemize}

For the calibration and scoring checks, the current estimator configuration was
deliberately set to \texttt{scipy} while the saved model recorded \texttt{lm}.
Both paths requested \texttt{("lm", 8)}, proving that the backend name was not
silently taken from the current estimator configuration.

The focused estimator, training, diagnostic, calibration, and scoring suites
reported:

\begin{verbatim}
66 passed
\end{verbatim}

The complete project regression suite reported:

\begin{verbatim}
207 passed, 10 deselected
\end{verbatim}

\subsection{Digests After Change 008}

\begin{verbatim}
estimator_factory.py:
42e6244cacfac042f02757ba86a9967251a4b975b9f0bd3b3c59d2d9ae6af2a2

modeling_stage.py:
79bf02524cb742408a4460ac8587aa56ebfd536f9f89aeda3da6dab00002b72d

cli.py:
f68fc459efafa8c933b5a5a4a3ecc2bffdc213b7ad97886f29e276419163fb95

calibration_stage.py:
174c144b388570778d770c2915055a5929462f572ed25a82e378e9a1d338742f

scoring_stage.py:
62c64906342e7e87631b32ffcd52589616d022c19d5386768b24363165d3ed6c

test_estimator_factory.py:
5ea05bbe48850de57ea7af43a965cb479ab9ef94113fedb7251d4cc47b77d879

test_modeling_stage.py:
8d6948dabff10a4f65a832b8880af6b57d355b0ad7c3e69617dae0ac8da5b0c3

test_cli.py:
c7788b773f887c84fdbfd7d9f1a727814f9a29875130fcc7d020b92ac4edbc59

test_calibration_stage.py:
c6eae75ad249b0ecefaee47ecfbd705ef38d550c6d742767d13325ad3ab14172

test_scoring_stage.py:
234532d56253ff88ff502cb0a4e1750f143ffb430882264c08ddaa3d68b97217

Runtime LM_time_lib.c (unchanged in this step):
60963e19d7e17daa8901192187142ba581a240064652286742ac2d1dcb550a05

Runtime LM_time_lib.so (unchanged in this step):
74c827713a213f0f750cf24319e0257fb2df595f530684bf9488b62c5b3d081c
\end{verbatim}

\subsection{Short Report for the Native-Code Authors}

This step changes only Python configuration wiring. The existing application
precision setting is now passed consistently to every LM construction path.
Calibration and scoring continue to select their backend from the fitted model
artifact. No native implementation, numerical method, coefficient, or compiled
library was changed.

\end{document}
